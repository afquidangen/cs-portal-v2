"use client"

// Use the '@/' alias to point directly to the components folder
import MyAccordion from "@/components/ui/shadcn/myaccordion"

export default function Page() {
  return (
    <MyAccordion />
  )
}